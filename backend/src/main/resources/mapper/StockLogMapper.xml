<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seckill.order.mapper.StockLogMapper">

    <insert id="insert" parameterType="com.seckill.order.domain.StockLog">
        INSERT INTO stock_logs
            (id, ticket_type_id, order_id, delta, type, created_at)
        VALUES
            (#{id}, #{ticketTypeId}, #{orderId}, #{delta}, #{type}, #{createdAt})
    </insert>

</mapper>
