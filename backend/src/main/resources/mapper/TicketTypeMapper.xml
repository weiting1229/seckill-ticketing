<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seckill.event.mapper.TicketTypeMapper">

    <!-- status 為 enum 對應 VARCHAR;price 為 BigDecimal 對應 NUMERIC;
         時間為 Instant 對應 TIMESTAMPTZ。map-underscore-to-camel-case 已開啟。 -->

    <sql id="cols">
        id, event_id, name, price, total_stock, stock_remaining,
        seckill_start, seckill_end, status, created_at, updated_at
    </sql>

    <insert id="insert" parameterType="com.seckill.event.domain.TicketType">
        INSERT INTO ticket_types
            (id, event_id, name, price, total_stock, stock_remaining,
             seckill_start, seckill_end, status, created_at, updated_at)
        VALUES
            (#{id}, #{eventId}, #{name}, #{price}, #{totalStock}, #{stockRemaining},
             #{seckillStart}, #{seckillEnd}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="findById" resultType="com.seckill.event.domain.TicketType">
        SELECT <include refid="cols"/> FROM ticket_types WHERE id = #{id}
    </select>

    <select id="findByEventId" resultType="com.seckill.event.domain.TicketType">
        SELECT <include refid="cols"/>
        FROM ticket_types
        WHERE event_id = #{eventId}
        ORDER BY id ASC
    </select>

    <select id="countByEventId" resultType="long">
        SELECT COUNT(*) FROM ticket_types WHERE event_id = #{eventId}
    </select>

    <update id="update" parameterType="com.seckill.event.domain.TicketType">
        UPDATE ticket_types
        SET name = #{name},
            price = #{price},
            total_stock = #{totalStock},
            stock_remaining = #{stockRemaining},
            seckill_start = #{seckillStart},
            seckill_end = #{seckillEnd},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM ticket_types WHERE id = #{id}
    </delete>

    <update id="markOnline">
        UPDATE ticket_types
        SET status = 'ONLINE', updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

</mapper>
