<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- 獨立模組,刻意不掛 backend 的 spring-boot parent:這支程式不需要 Spring,
         只要與正式程式碼「同一條限流路徑」的那幾個套件。版本一律對齊 backend 實際解析到的版本
         (見 backend/target 的 BOOT-INF/lib),否則量到的不是正式站在跑的那份程式碼。 -->
    <groupId>com.seckill</groupId>
    <artifactId>ratelimit-bench</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <properties>
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <bucket4j.version>8.19.0</bucket4j.version>
        <lettuce.version>6.6.0.RELEASE</lettuce.version>
        <netty.version>4.1.137.Final</netty.version>
    </properties>

    <dependencies>
        <!-- 與 backend 同版本、同套件:bucket4j_jdk17-lettuce 的 casBasedBuilder 就是正式站在走的路徑 -->
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j_jdk17-core</artifactId>
            <version>${bucket4j.version}</version>
        </dependency>
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j_jdk17-lettuce</artifactId>
            <version>${bucket4j.version}</version>
        </dependency>
        <!-- 明確釘住,不讓 bucket4j 的傳遞相依把版本帶到跟 backend 不一樣 -->
        <dependency>
            <groupId>io.lettuce</groupId>
            <artifactId>lettuce-core</artifactId>
            <version>${lettuce.version}</version>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <!-- netty 的子模組必須整組同版本,只釘其中幾個會在執行期炸 NoSuchMethodError
                 (實測:netty-common 與 netty-buffer 版本不一致 → PlatformDependent.isExplicitNoPreferDirect
                 找不到)。用 BOM 一次對齊到 backend 解析到的 4.1.137.Final。 -->
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-bom</artifactId>
                <version>${netty.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <finalName>ratelimit-bench</finalName>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
            <!-- BENCH_MODE=lua-purchase 直接打包 backend 的正式腳本,不複製一份:量到的就是會上線的那支(ADR 0010 §9) -->
            <resource>
                <directory>../../backend/src/main/resources/lua</directory>
                <includes>
                    <include>ratelimit_token_bucket.lua</include>
                </includes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals><goal>shade</goal></goals>
                        <configuration>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>com.seckill.bench.GlobalKeyBench</mainClass>
                                </transformer>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                            </transformers>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                        <exclude>module-info.class</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
